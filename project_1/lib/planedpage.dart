import 'package:flutter/material.dart';

class planed_Page extends StatelessWidget {
  const planed_Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Kế hoạch đã lập'),
        backgroundColor: Colors.green[700],
      ),
    );
  }
}