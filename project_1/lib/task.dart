import 'package:flutter/material.dart';

class myTask_Page extends StatelessWidget {
  const myTask_Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tác vụ'),
        backgroundColor: Colors.green[700],
      ),
    );
  }
}